{
  "name": "top-articles",
  "version": "2.0.0",
  "private": true,
  "description": "Statisch overzicht van mijn Readwise-toplijsten, met directe links naar Readwise Reader.",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "check": "npm run lint && npm run typecheck && npm test",
    "build": "npm run compile && node dist/scripts/build-data.js",
    "priority:judge": "npm run compile && node dist/scripts/priority-judge.js",
    "priority:plan": "npm run compile && node dist/scripts/priority-cli.js plan",
    "priority:apply": "npm run compile && node dist/scripts/priority-cli.js apply",
    "priority:verify": "npm run compile && node dist/scripts/priority-cli.js verify",
    "priority:topic-report": "npm run compile && node dist/scripts/topic-priority-report.js",
    "priority:interest-report": "npm run compile && node dist/scripts/core-interest-report.js",
    "archive:plan": "npm run compile && node dist/scripts/archive-cli.js plan",
    "archive:apply": "npm run compile && node dist/scripts/archive-cli.js apply",
    "archive:verify": "npm run compile && node dist/scripts/archive-cli.js verify",
    "archive:lees:plan": "npm run compile && node dist/scripts/archive-lees-cli.js plan",
    "archive:lees:apply": "npm run compile && node dist/scripts/archive-lees-cli.js apply",
    "archive:lees:verify": "npm run compile && node dist/scripts/archive-lees-cli.js verify",
    "archive:cleanup:plan": "npm run compile && node dist/scripts/archive-cleanup-cli.js plan",
    "archive:cleanup:apply": "npm run compile && node dist/scripts/archive-cleanup-cli.js apply",
    "archive:cleanup:verify": "npm run compile && node dist/scripts/archive-cleanup-cli.js verify",
    "email:core-interest": "npm run compile && node dist/scripts/send-core-interest-email.js",
    "test": "npm run compile && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.9.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  }
}
