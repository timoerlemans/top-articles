{
  "name": "top-articles",
  "version": "1.0.0",
  "private": true,
  "description": "Statisch overzicht van mijn Readwise-toplijsten, met directe links naar Readwise Reader.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-data.mjs",
    "priority:plan": "node scripts/priority-cli.mjs plan",
    "priority:apply": "node scripts/priority-cli.mjs apply",
    "priority:verify": "node scripts/priority-cli.mjs verify",
    "test": "node --test test/*.test.mjs"
  }
}
